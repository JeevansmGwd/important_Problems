#include <stdio.h>
int main()
{
int p,n,r,result;
printf("enter the value of p: ");
scanf("%d", &p);
printf("enter the value of n: ");
scanf("%d", &n);
printf("enter the value of r: ");
scanf("%d", &r);
result=p*n*r/100;
printf("%d", result);

    return 0;
}
